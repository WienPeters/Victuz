@model IEnumerable<WPCasusVictuz.Models.Picture>

@{
    ViewData["Title"] = "Foto's van Victuz";
}

<h1>Foto's van Victuz</h1>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="thumbnail">
                <a asp-action="Details" asp-route-id="@item.Id">
                    <img src="@item.FilePath" alt="Uploaded Picture" style="width:100%">
                </a>
                <div class="caption">
                    <p>
                        Geupload door:
                        @if (item.AddedByBoardMember != null)
                        {
                            @item.AddedByBoardMember.Name
                        }
                        else
                        {
                            <span>Unknown</span>
                        }
                    </p>
                </div>
            </div>
        </div>
    }
</div>
